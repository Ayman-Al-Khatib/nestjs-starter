import { defineConfig } from 'eslint/config';
import js from '@eslint/js';
import globals from 'globals';
import tseslint from 'typescript-eslint';

export default defineConfig([
  {
    ignores: [
      'dist/**',
      'node_modules/**',
      'coverage/**',
      // Auto-generated by `npm run i18n:sync` — never hand-edited, never linted.
      'src/infrastructure/i18n/translation-keys.ts',
    ],
  },
  { files: ['**/*.{js,mjs,cjs,ts}'], plugins: { js }, extends: ['js/recommended'] },
  {
    files: ['**/*.{js,mjs,cjs,ts}'],
    languageOptions: { globals: { ...globals.node, ...globals.jest } },
  },
  tseslint.configs.recommended,
  {
    files: ['**/*.ts'],
    rules: {
      // Underscore-prefixed bindings are an explicit "intentionally unused" marker.
      '@typescript-eslint/no-unused-vars': [
        'error',
        { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
      ],
    },
  },
  {
    // Baileys exposes a loosely-typed, dynamically-loaded socket/auth surface;
    // pinning exact types here would mean re-deriving the library's internals.
    files: [
      'src/modules/whatsapp/services/whatsapp.service.ts',
      'src/modules/whatsapp/utils/baileys-auth-state.util.ts',
    ],
    rules: { '@typescript-eslint/no-explicit-any': 'off' },
  },
]);
